import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {marginVertical: 10, marginHorizontal: 20},

  value: {
    fontWeight: '500',
    fontSize: 16,
    color: '#000',
  },
});
export default styles;
